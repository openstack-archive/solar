---

id: images
type: resource
driver: playbook
version: v1
run:
    - shell: docker pull {{item}}
      with_items: images.list
parameters:
    list:
        required: true
    another:
        default: 1
