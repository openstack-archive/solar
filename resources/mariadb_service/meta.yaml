id: mariadb
handler: ansible
version: 1.0.0
input:
    image: 
      schema: str!
      value: kollaglue/fedora-rdo-mariadb-app
    root_password:
      schema: str!
      value: password
    port:
      schema: str!
      value: 3306
    ip:
      schema: int!
      value:
    ssh_key:
      schema: str!
      value:
    ssh_user:
      schema: str!
      value:

tags: [resource/mariadb, resources/mariadb]
