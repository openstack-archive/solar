id: mariadb
handler: ansible
version: 1.0.0
input:
    image: 
      schema: str!
      value:
    root_password:
      schema: str!
      value:
    port:
      schema: str!
      value:
    ip:
      schema: int!
      value:
    ssh_key:
      schema: str!
      value:
    ssh_user:
      schema: str!
      value:
