# This resource will clean
handler: ansible
version: 1.0.0
input:
    ip:
      schema: str!
      value:
    repos:
        schema: [str!]
        value:
    names:
        schema: [str!]
        value:
    ensure_other_removed:
        schema: bool
        value: true
