id: lxc_container
handler: ansible_playbook
version: 1.0.0
actions:
input:
    ip:
      schema: str!
      value:
    ssh_key:
      schema: str!
      value:
    ssh_user:
      schema: str!
      value:
    ansible_ssh_host:
      schema: str!
      value:
    container_address:
      schema: str!
      value:
    container_name:
      schema: str!
      value: